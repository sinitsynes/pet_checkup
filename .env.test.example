# Test Database Configuration
# Copy this file to .env.test and update with your test database credentials

# Database Host
TEST_DB_HOST=localhost

# Database Port (default Docker Compose uses 5433 to avoid conflicts)
TEST_DB_PORT=5433

# Database User
TEST_DB_USER=postgres

# Database Password
TEST_DB_PASSWORD=postgres

# Database Name
# Consider using a separate database for tests to avoid affecting dev data
TEST_DB_NAME=pet_checkup_test

# Full connection string (optional - will be constructed from above if not set)
# TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5433/pet_checkup_test?sslmode=disable

# Test Configuration
# Set to 1 to skip integration tests when running `go test -short`
# GO_TEST_SHORT=0

# Logging Level for Tests (optional)
# TEST_LOG_LEVEL=debug
